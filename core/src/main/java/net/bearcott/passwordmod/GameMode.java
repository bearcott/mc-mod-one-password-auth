package net.bearcott.passwordmod;

/**
 * Game modes, named exactly like vanilla's so the sessions file stays readable by every version
 * of the mod ("originalMode": "SURVIVAL").
 */
public enum GameMode {
    SURVIVAL,
    CREATIVE,
    ADVENTURE,
    SPECTATOR
}
